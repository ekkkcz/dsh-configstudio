# 变更记录

## 0.6.0 —— M4 发布准备：干净安装、升级卸载、完整回归、一处 A18 实测缺陷

这一版是 **M4 发布准备**的产物（仍是**阶段试玩版**，不是完整 V1）。
它主要是"把该验的都验一遍"：全新 profile 上装交付包按真实用户路径走完、
0.4.0 → 0.5.0 的升级与降级边界、全部验收套件一次跑齐。
过程中**撞出一个真缺陷**并修掉（见下），所以有代码改动、需要新版本号。

### 1 修掉的真缺陷：失败的调用显示成"0 tok"

**现象**（在真实实例的对比页上实测到的，不是推测）：一个额度不足（QUOTA）的候选，
作品卡上写着 `输入 0 tok / 输出 0 tok / 合计 0 tok / 总速度 0.0 tok/s`。
这直接违反 A18 —— 要求是"显示未上报或未知，**不显示零 Token 零费用**"。

**根因不在界面**：宿主的 `TokenUsage` 把 `inputTokens` / `outputTokens` 声明成**必填 number**
（类型上就没有 null 这一说）。一次没有成功收尾的调用（额度不足 / 鉴权失败 / 上游 500 /
取消 / 超时）根本没有用量可报，适配器只能填 0 —— 那个 0 是"类型不允许表达未知"的产物，
**不是**"模型说用了 0 个 token"。我们照抄了它，于是把占位值当成了真实数据。

**修法**：新增 `src/core/usage.js`，把口径写成**一份**、两处调用：

| 调用方 | 作用 |
| --- | --- |
| 写入侧 `core/runner.js` | 新产生的数据当场归一：没成功收尾 + 全 0 → 记为"未上报" |
| 读取侧 `api.js` 的 `describeAttempt` | **历史数据与导入包**里的旧记录读出来也一并修正 |

判据（写进测试钉死）：成功收尾的调用**一个字都不改** —— 上游真报 0 那是它的口径；
失败但有非 0 用量的照实保留；只有"没成功收尾 且 全 0"才判为未上报，
并在记录里留下 `unreportedBecause` 说明，免得后来的人以为是我们丢了字段。
**实测**：修前界面显示 `0 tok`，修后显示 `未上报 tok`（同一份旧数据，读取侧修好的）。

### 2 干净安装（A01 补齐"真实用户路径"）

- 全新 profile（`arena-m4clean`）装交付 tgz → 接口与界面走查 **24/24**；
- **完整用户路径**：生成 → 对比 → 盲选 → 评价 → 揭晓 → 导出展示包/复测包 → 历史，
  **18/18**，且整条路径**只访问本机回环地址**（0 个对外请求）。
- 零费用怎么做到：新增 `scripts/sim-llm-overlay/`（一个几十行的 DSH 插件 + overlay），
  用 `dsh --patch` 把 `scripts/simulated-llm.mjs` 注册成 provider —— 装的是**交付的 tgz**，
  跑的是**真实 DSH**，但模型是模拟的、不联网。overlay 不改 profile 的 package.json。

### 3 升级 / 卸载 / 回退边界（A29 补齐"真路径"）

`scripts/m4-upgrade-uninstall-check.mjs` **44/44**，全程在临时 profile 里做、不碰用户现有配置：

- 装 **v0.4.0** → 真的跑出数据（schema **2**，没有 `pack_imports` 表）；
- 换成 **v0.5.0** → 自动迁移到 schema **3**，只加表不改列，**attempt 的正文指纹与作品 HTML 指纹逐条一致**；
- **回退边界**：把 0.4.0 装回去 → **明确拒绝**打开（"schema 3 > 2，请升级插件后再打开"），
  且拒绝之后数据目录仍是 schema 3、行数未变（**没有被老版本动过**）；
- 卸载 → bundles 里不再出现本插件，但**数据目录与作品文件都还在**；
- 重装 → 那条 0.4.0 造的实验仍在列表里、界面能打开。

为了让"升级验证"验的是**那份装出来的代码**（而不是仓库里当前这份），
`scripts/dev-server.mjs` 新增 `--root <目录>`：产品代码全部从该目录动态加载，一份都没有抄进脚手架。

### 4 完整回归（一次跑齐，不再手敲）

新增 `scripts/m4-full-regression.mjs`：把 `tests/` 与 `scripts/` 下的套件一次跑完，
逐条打印结果，最后给出**A01–A30 的逐条覆盖表**并点名"没有被任何套件覆盖的必需项"。
跑的过程中实时写 `m4-full-regression-live.json`，中途被打断也能看到跑到哪了。

**本轮结果：29 套全部通过、0 失败、A01–A30 覆盖 30/30。**
（依赖的服务没起时会明确报"缺服务"并跳过，不会伪装成失败或通过。）

### 5 A04 / A18 的如实结论

- **A04**（参数不受模型支持）：新增 `scripts/m4-a04-adapter-probe.mjs` **15/15**，
  用**两个独立口径**查（对着实例打 `/models/resolve` 看真实字段 + 读 DSH 安装里的类型声明），
  结论："温度**完全没有**能力字段；输出上限只有 `defaultMaxTokens` 这个**默认值**口径，
  不是'支不支持输出上限'。" 界面显示"未指定"是**与接口现状一致**的诚实做法。
- **A18**（模型不返回 Token）：新增 `scripts/m4-a18-usage-scan.mjs` **9/9**，
  扫全部 **73 个真实模型 attempt**：目前**仍未遇到**真实模型不上报用量的情形，
  因此 **A18 保持"未测"**，不用模拟结果顶替。但这次扫描发现了第 1 节那个**真缺陷**并已修掉。

### 6 文档与演示材料

- `README.md`：版本、能力边界、已知限制（能力 / 环境 / 降级三类分开写）、"改动之后跑什么"。
- `docs/architecture.md`（新增）：**模块与数据流** —— 三个进程怎么分工、一次点击数据落到哪、
  数据目录与 schema、脚本分工、"想改一处去哪找"。
- `docs/third-party-notices.md`（新增）：第三方来源与许可证 —— 结论是**本插件不含任何第三方运行时代码**
  （0 个 npm 依赖、前端无构建步骤），需要留意的只有你选的 CDN 上那些库与可选的 Playwright/Chromium。
- `scripts/m4-demo-record.mjs`（新增）：按首发演示路线走一遍并录屏，
  产出 11 张分镜 + 一段 webm。**没录成会明说没录成**，不假装。

### 7 发布前隐私自检（这次抓到两个真问题）

1. **历史里有本机用户主目录绝对路径**：`docs/evidence/m0-dsh-boot.txt` 是**手写**的证据文本，
   从来没走过脱敏；`scripts/lib/sanitize-evidence.mjs` 那时**只扫 `.json`**，于是它一路留了下来。
   已修根因（**.txt 也扫**），并用 `scripts/m4-history-scrub.mjs` 把三类痕迹
   （用户主目录路径 / 与本插件无关的本机程序名 / 临时访问令牌）从**全部提交**里抹掉。
2. **提交身份有三种写法**（都是中性的，但看起来像三个人）：已统一成
   `HTML Arena Dev <dev@html-arena.local>`。

> 这次自检的价值在于说明一件事：**"我搜过了"和"规则覆盖到了"是两件事** ——
> 漏掉的不是搜索结果，是**搜索范围**（只扫 JSON、只扫工作区不看历史）。

## 0.5.0 —— M3 复用与分享：展示包与复测包、历史筛选、受控 CDN 实测

这一版是 **M3 阶段试玩**（仍不是完整 V1）：把"配方跨安装复用、作品与配置能分享给别人、
历史能查"这几件事做成可核对的功能，并用**两个干净数据目录之间的真实往返**验证。

### 1. 展示包与复测包（F20 / F21 / F22，A24–A27）

- **展示包**：一个可双击打开的 ZIP —— `index.html` 报告（对比阅读 + 人工评价 + 下载入口）、
  `works/*.html`（作品原文）、`shots/*.png`（导出时**重新加载**作品采集的初始截图）、
  `shots/records.json`（截图记录，含失败那几次）、`pack.json`（公开元数据与每个文件的 sha256）。
  报告**一行脚本都没有**，作品走 `<iframe sandbox="allow-scripts">` 受限预览（不给同源权限），
  引用 CDN 的作品在报告里被标成"需要联网"（按作品内容判定，不猜）。
- **复测包**：版本化 ZIP，装题目 / 可选起始 HTML / 每个候选的配方 / 预览与输出规则 /
  `schemaVersion` / 内容 hash。**导入时重新匹配模型**（本机没有的模型会逐个列出来让你重选，
  绝不替你猜一个顶上），**不会自动发起任何调用**（导入前后模型调用日志行数一致）。
- **导入前先检视**：`/packs/inspect` 只读解析，显示包里有什么、题目指纹、每个候选在本机的
  匹配结果与"将要创建什么（1 条实验 + N 个配方 + 0 次模型调用）"；确认后才写库，
  写库在**一个事务**里（中途失败不留半成品实验）。
- **导入校验**：路径穿越 / 反斜杠归一化后的穿越 / 绝对路径 / 盘符 / NTFS 备用数据流写法 /
  结尾点空格 / Windows 保留名 / 大小写与 Unicode 归一化重名 / 可执行载荷 / 压缩炸弹 /
  ZIP64 / 加密 / 多卷 / 本地头与中央目录名字不一致 —— 全部明确拒绝。
  **压缩炸弹的防护落在解压本身**（`inflateRawSync` 的 `maxOutputLength`）：
  早先三条闸门读的都是"声明的大小"，而声明是可以撒谎的（实测：声明 1KB、实际解出 128MB）。
- **导出的内容边界**：只脱敏**我们生成的元数据**；题目与配方正文**逐字节照抄**
  （改了它们，题目的 hash 在导出前后就对不上了）。用户内容里若出现本机路径，
  导出前会**如实提示**"这是你自己的内容、插件不会替你改写，不确定就把勾去掉"。

### 2. 历史搜索与筛选

- 新增**按评价筛选**：已评价 / 未评价 / 已揭晓 / 未揭晓 / 偏好 A–D / 平局 / 无法判断，
  可与标题搜索、类型筛选组合；判定在服务端做（与界面同一口径）。
- 列表行新增「导出」入口；从复测包导入的实验会标出"由复测包导入（N 个候选需重新选模型）"。

### 3. 受控 CDN 的真实网络验证（A17）

- offline 策略下 CDN 资源被 CSP 挡下（对该域**没有任何响应**，控制台写明违反的指令），
  作品自己的脚本确实失败且**看得见**（页面异常 + 作品里 `data-three="error"`）；
  受控 CDN 策略下同一个作品拿到 **HTTP 200**、`THREE` 可用。
- 两种情况都**不改作品代码**（前后作品 HTML 逐字节一致），截图记录如实带上页面异常计数。

### 4. 数据 schema 2 → 3

- 新增 `pack_imports` 表（导入来源与模型匹配结果留痕）。仍是**只加不改**的增量迁移。

### 5. 由独立只读核查发现并修掉的问题（都有回归测试）

- 导出时"先算 hash 再脱敏"导致**自己导出的包被自己拒收**（指控"包被改过"）；
- 脱敏的路径模式套在 HTML 上会吃掉闭合标签 → 报告可能**整页空白**、作品被截断；
- 压缩炸弹防护建立在声明值上；
- 导入采信 `pack.json` 的任意数字（`timeoutMs=1e15` 会让实验每次运行都立刻超时）；
- 只校验中央目录的名字（本地头可以写成穿越名字）；
- 畸形包得到 500 而不是 400；导入不是原子的；
- 报告里的 `src/href` 没有 scheme 白名单。

这一版是 **M2 阶段试玩**（不是完整 V1）：把"配置快照与配方 / 取消超时 / 重启恢复 / 初始截图"
这几件事从"实现了"推进到"有实测证据"。四轮反馈形成的界面打磨没有重做。

### 1. 配方对象与版本化（A03 原先"部分通过"的那一半）

以前只有"每次尝试里的配方快照"，没有**配方对象**，所以"历史配方不被覆盖"无从验证。

- 新增 `recipes` / `recipe_versions` 两张表（数据 schema 1 → 2，**增量迁移**，老数据目录原样保留）。
  版本**只追加**：代码里没有任何 UPDATE 已有版本的路径；每一版自带 `content_hash`。
- 内容口径只有一份（`src/core/recipe.js`）：只把"会影响这次调用"的字段算作配方内容，
  槽位/时间戳不参与指纹。内容与当前版完全一致时**不新增版本**，如实回 `unchanged`。
- attempt 里存的始终是**启动时那一版的快照**；`recipe_id`/`recipe_version` 只作溯源，
  所以配方后来怎么改都不会回头改写历史实验（F19）。
- 界面新增「配方」页：列出每个配方的全部版本，历史版本标注"历史，只读"，
  可直接"用这一版新建对比"；候选卡上写明"来自配方 X 第 N 版"。

### 2. 宿主重启恢复（A09，原先"未测"）

- `recoverUnfinishedAttempts()` 抽到 `core/runtime.js`，**宿主半边与开发服务器共用** ——
  原先只有 `src/index.js` 做了这件事，于是"重启后未完成仍标 running"这个缺陷在零费用的
  开发服务器上根本复现不出来（与当年"实时流缓冲只有宿主半边有"是同一类错误）。
- 被**硬杀**时，进程里的实时缓冲会一起消失，而 `raw.txt` 要等流结束才写 ——
  原先"生成到一半被杀"留下的是一条没有任何正文的记录。现在生成过程中每 1.5 秒把已收到的正文
  刷到 `partial` 文件（上限 256KB），跑完就删，`raw.txt` 仍是唯一权威正文。
- 实测（`scripts/m2-restart-recovery-check.mjs`，23/23）：SIGKILL 之后重启，
  未完成的那次是 `interrupted`、已完成作品正文逐字节不变、**没有新建 attempt、
  模型调用日志行数不变**（不自动重付费）；用户显式点重试才会新建一轮。

### 3. 超时与取消（A08 原先"部分通过"）

- `outputPolicy.timeoutMs` 以前只是**存下来**，从没有人用它中止调用。现在按本轮上限计时，
  到点尽力 abort，并记成 `timed_out` + `finishReason=timeout` + `errorCode=TIMEOUT`。
- **超时与取消分开记录**：`timed_out` vs `cancelled`，原因与下一步都不同，互不覆盖。
- 到点但流还没收尾时，`/live` 报 `timedOut: true`，界面写"已到运行上限，正在尽力中止…"。
- "不污染新 attempt"用可核对的口径验证：同批放一个忽略中止的候选，
  正常候选 0 处沾到它的迟到内容；窗口内新开的那一轮正文长度等于**一次**完整输出，
  且其正文/作品 hash 都能从磁盘内容重新算出来。

### 4. 截图失败的正式记录（A23 原先"仍未测"的那一半）

- 新增截图记录表：成功与失败都落一条（视口、状态、原因、耗时、页面诊断计数），
  **只记状态与原因，不记图片本身**。
- 实测：死循环作品截图 `status=timeout`（"子进程超过硬上限 15000ms 被强制终止"），
  15 秒内返回、记录落盘、刷新页面仍可见；成功那次也有记录；失败前后作品 hash 完全一致。
- 顺带修掉两个由断言抓出来的界面缺陷：把"当时成功但没存图"的记录渲染成
  "截图未成功：未知原因"（假信息）；以及把属于上一轮的记录显示成一串 attempt id。

### 5. 未识别的错误不再只说"未识别"（A07 记下的已知不足）

`explainError()` 对任何错误码都带上上游原始 `message`，界面放进可展开的
"上游原始错误（PI_AI_ERROR）"。上游 404 的真实原因（`Model "…" does not exist.`）
从此在运行面板与对比页都看得到。

### 6. 顺带修掉的界面缺口

- 对比页卡片头对**非 completed** 的候选加上状态标记（超时/取消/中断/失败），
  原因放进 title；"展开配置"里补上"本轮状态 / 收尾原因 / 失败原因 / 上游原始错误 / 部分输出"。
  （超时的作品也可能有 HTML，只看作品是看不出它超时了的 —— 这是 A08 的界面断言抓出来的。）

### 验收

`node --test "tests/**/*.test.js"` **107 / 107**（原 95 + 配方 6 + 稳定性 6）；
新增四个零费用验收脚本：重启恢复 **23/23**、超时与取消 **24/24**、截图失败记录 **19/19**、
配方版本界面全流程 **15/15**；原有全部套件重跑通过（见 `docs/acceptance.md` 的回归基线）。

---

## 0.3.2 —— 作品帧自适应撑满（用户第四轮反馈，1 条）

用户原话（配截图）："这个手机这里怎么右边一堆空白，得自适应"。

### 缺陷

对比页切到**手机逻辑视口**后，作品右边留下一大条空白。根因是缩放公式：

```js
scale = Math.min(1, avail / vp.width)   // ← 错
```

手机逻辑视口只有 **390px**，而卡片有 400–490px 宽。"取 min 1"意味着**永远不放大** ——
作品就按 390px 原尺寸画出来，右边剩下约 **98px 死白**（实测：卡片 488 / 作品 390 / 空白 98）。
同一个公式还导致宽屏上更难看：1600px 窗口下手机视口要撑满卡片需要 1.9×，
被卡在 1（后来把上限放到 1.6，空白仍有 118px）。

### 修法

抽出**唯一口径** `frameScale(vpWidth, avail, wide)`，`buildFrame()` 与 `refitFrames()` 共用
（以前两边各写一份，正是漂移出这个缺陷的原因）：

- 「1:1 横向展开」且内容比容器宽：保持 **1:1**，横向滚动交给容器；
- **其余情况：等比缩放正好撑满容器宽度**（小了放大、大了缩小），**不留一点空白**。

**为什么最终没有给放大留上限**：留了上限就必然在窗口更宽时重新露出空白 ——
实测 1.6 倍上限在 1600px 窗口下又留下 118px，等于把缺陷挪到别的分辨率上。
"撑满"必须是一条没有例外的规则；想看原始尺寸是「1:1 横向展开」的职责。

同时把当前缩放倍数与逻辑视口宽度**写在界面上**
（"正好撑满、不留空白（当前 1.9×，390px 逻辑视口）"）—— 宽屏上手机视口会被放大到 1.9×，
用户有权知道这是放大后的样子，而不是真实像素尺寸。

### 验收

`scripts/m2-horizontal-compare-check.mjs` 从 18 项扩到 **22 项**，新增 4 条断言：
1600 / 1024 / 880px 三种窗口下，手机视口**作品渲染宽度 == 卡片宽度、空白 ≤ 2px**；
以及手机视口下切到 1:1 也不留白（本来就没有可滚内容）。**22 / 22 通过，0 控制台错误。**


## 0.3.1 —— 第三轮用户试玩反馈（阶段试玩，非完整 V1）

用户在试玩 v0.3.0 后提了 2 条，都属于**当前范围内的体验改进**，本版全部落地。
**服务端一个字节都没改**（两条都是界面问题），因此数据目录 schema 未变、旧实验记录继续可用。
**F02（单候选单次逻辑生成请求）没有被改写。**

### 反馈 1：对比页要能"水平展开比对"

用户原话："这个得加一个水平展开比对的功能"。

**根因（上一轮已实测定位，本版修掉）**：`web/app.css` 里有一条
`@media (max-width: 900px) { .compare-grid { grid-template-columns: 1fr } }`。
用户截图是 1983 物理像素，但他那块屏 DPR ≈ 2.25 → **CSS 视口只有约 880px** → 命中该断点 →
插件把"并排比对"这个核心动作自动关掉了。**这不是用户不会用，是真实的布局问题。**

修法（三处，都为"始终并排"服务）：

1. **列数改由代码写死**（`renderCompare()` 直接写 `gridTemplateColumns`）：
   1 个候选 1 列，**2–4 个候选永远是每行 2 个**，同行等分等宽。CSS 里那条断点已删除。
2. **窄视口下给了两种读法**（新增"横向展开比对"工具条，两个开关故意分开）：
   - 「缩小看全」（默认）：整份作品等比缩进卡片，不用拖动就能看个整体；
   - 「1:1 横向展开」：作品按 1280px 原始宽度摆开，每张卡片**各自横向可拖**，
     字和按钮恢复原尺寸 —— 窄屏上真正能看清细节的读法；
   - 「左右同步滚动」（默认开）：一边拖到哪，另一边跟到同一**比例**位置
     （按比例而不是抄像素：网格与卡片容器的可滚距离并不相等，抄像素会让左右错开）。
3. **"全屏"不再和"并排"打架**：旧实现点全屏会把网格类改成 `compare-grid single`（= 单列），
   与"始终并排"直接冲突。现在全屏只隐藏其它卡片 + 让这张跨满整行，列数不变；
   并且卡头双击也能放大/恢复。

### 反馈 2：最终页面要显示 token 消耗、速度等数据

用户原话："然后最终页面也要写上消耗 token，速度，等数据"。
数据**服务端一直都返回了**（v0.3.0 也渲染了），只是藏在默认折叠的「展开配置」里 = 用户看不见。

- 现在每个作品卡**头部正下方**就有一条用量摘要：输入 / 输出 / 合计 / 推理 tokens、
  **总速度**、**首正文延迟**、总耗时。不用展开任何面板。
- **速度口径是这个版本最容易做错的地方**（上一轮实测踩过）：
  - ✅ **总速度 = outputTokens ÷ (finishedAt − startedAt)** —— 整个调用的墙上时间。
  - ❌ **绝不能**用 `outputTokens ÷ (finishedAt − firstTextAt)`：推理型模型会先把大量
    reasoning token 吐完才出正文，用户那条真实实验里"等首正文"花了 **168.3 秒**、
    正文只写了 11.1 秒，拿后者当分母会算成 **3703 tok/s**（明显失真）。
  - 所以界面**同时显示两个口径**并写明含义：总速度 + 首正文延迟。
    反例值已被验收脚本断言"不得出现在页面上"。
- `reasoningTokens` 实测为 `null`（服务端没上报）→ 按既有约定写**"未上报"，绝不写 0**（F18 / A18）。
- **盲选指纹判定**（用户要求判断的部分）：token 数与速度**不像**上下文窗口那样与某个模型一一对应
  （换个题目数字全变），因此不是强指纹；但两点仍然成立：① 它们与上下文窗口 / 档位清单同属
  "调用元数据"，同一块面板里一半遮一半露很怪；② 盲选要的是"先看作品"，
  把 235.8 tok/s 摆出来会把对比变成跑分、反过来影响投票。
  所以**按既有约定整块隐藏**并写明"揭晓后可见"。
- **顺带修掉一个真实泄露**：v0.3.0 的「展开配置」在盲选期间**仍然把 token 数写出来了**
  （旧代码只遮了 provider/model/指纹，用量那一块没遮）。现在同一约定覆盖到它。

### 工程改动

- `web/app.js`：`refitFrames()` / `syncScrollFrom()` / `applyCompareMode()` / `buildUsageStrip()` /
  `totalSpeed()`；`renderCompare()` 写死列数并渲染用量摘要；`fullscreen()` 不再改网格类。
- `web/app.css`：删掉造成堆叠的媒体查询，新增 `.usage-strip` 与 1:1 横向展开的滚动样式。
- `web/index.html`：新增"横向展开比对"工具条（模式 + 同步滚动开关）。
- **开发期间修掉的两处旧脚本自身缺陷**（都会造成"假失败/假结论"，与代码无关）：
  1. `m1-compare-walkthrough` 的前置条件拿"轮次行数"要求"候选数"——
     追加过一轮的实验有 3–4 行，会误报失败；改为**按候选槽位取最新一轮**再判定。
  2. `m2-ui-walkthrough` 断言"默认关"写死在脚本里，而设置是持久化的：
     上一次演练把它打开后，再跑就必然失败；改为**断言开关状态与可见性一致**，
     "默认关"由 `m2-capability-walkthrough` 用"关掉再试"的路径证明。
     更进一步：该断言原先会**重新 fetch 服务端设置**来比对本页面的可见性 ——
     与另一个脚本并行跑时，服务端被改了而本页面还没重新加载，就会报假失败。
     现在改为断言"界面可见性 == **这个页面自己依据的**（探测到 且 已启用）"，不跨进程读状态。
  3. `m2-usage-metrics-check` / `m2-four-walkthrough` 的指纹断言用朴素 `includes` 会**误报**：
     某条真实实验的输出 token 数是 **81**，页面上到处是时间戳与耗时，`"81"` 作为更长数字的
     一部分偶然命中，报了一次"盲选泄露指纹"的假警。已抽成 `fingerprintHit()`：
     **纯数字要求两侧都不是数字**才算命中，带字母/符号的指纹照常 substring。
- 三处脚本默认端口从 **8901 改成 8902**（8901 已被本机无关程序占用，
  不带 `--base` 会打到别的服务上、得到莫名其妙的失败）。
- `scripts/m2-layout-probe.mjs` 修掉自己的**错误口径示范**：以前它算并打印的是
  "首正文之后的时间当分母"那个失真值，还把它命名为 `tokPerSec`、并在结论里推荐使用。
  现在明确拆成 `totalSpeed`（界面口径）与 `wrongSpeed_DO_NOT_USE`（只作对照）。

### 验收变化

- 新增 `scripts/m2-horizontal-compare-check.mjs`：**18 / 18 通过**（含 DPR 2.25 + 880 CSS 像素的真实复现）。
- 新增 `scripts/m2-usage-metrics-check.mjs`：**21 / 21 通过**
  （含"错误口径的值不得出现"与"用量未上报必须写未上报"两条反例断言）。


## 0.3.0 —— 第二轮用户试玩反馈（阶段试玩，非完整 V1）

用户在试玩 v0.2.0 后提了 4 条。其中第 1 条是**真缺陷**（重绘丢用户操作状态），
第 4 条是界面缺口，第 2 条是设计问题（插件的选择权），第 3 条是需要先拍板范围的范围补充。
**F02（单候选单次逻辑生成请求）没有被改写。**

### 修复的缺陷

- **运行面板点开"推理过程"不到 1 秒就自己收回去**（用户第 2 条反馈，真缺陷）。
  根因：轮询每 700ms 调一次 `renderRun()`，而它开头就是 `clear(box)` —— 整个候选区被全量重建，
  展开状态必然丢失。影响不止"推理过程"：任何展开的 `<details>`（含"原始输出与提取结果"）、
  实时输出区的滚动位置、面板里选中的文字都会在下一个轮询周期被换掉。
  修法：`renderRun()` 改为 **keyed 增量更新** —— 卡片骨架只在候选集合变化时重建，
  文本一律原地改写文本节点的值；实时输出只在"用户本来就贴着底部"时才自动吸底。
- **截图会重置用户正在操作的作品**（同类根因，审查时发现）：
  `takeScreenshots()` 每完成一张截图就调一次 `renderCompare()`，四候选就是 4 次重建 iframe。
  已把截图面板拆到独立的 `#screenshot-panel`，**不碰作品网格**。
- **保存评价后刷新页面，标签与理由变空**（服务端其实存了，界面不回填）。
- **`buildFrame()` 的 resize 监听器只加不减**：每次重绘累积，闭包捕获已脱离 DOM 的节点。
- 顺手修：搜索框的请求竞态（加序列号守卫 + 180ms 防抖）、
  卡片里 `firstText` 的陈旧文本、`openExperiment()` 未显式清理增量渲染引用。

### 新增

- **设置页与外部插件能力开关**（用户第 1 条反馈："得让用户自己选择是否加插件呀"）。
  - 提示词优化是另一个插件的能力，现在**默认关**，要用户在「设置」里显式打开；
    探测到 ≠ 启用，界面与服务端**两层**都拦住（关闭时直接打接口也会被拒绝）。
  - 新建**跟随数据目录**的设置持久化（`$DSH_HOME/html-arena/settings.json`），
    **不用 localStorage** —— 换浏览器/换入口行为一致，M3 的展示包也能带走。
- **对比页"展开配置"**（用户第 4 条反馈）。以前只渲染 6 个字段，现在能看到：
  题目原文与输出要求、taskHash、运行上限与并发数、预览网络策略、
  每个候选的系统提示词与提示词片段、上下文窗口与可用思考档位、
  用量（缺失写"未上报"，绝不写 0）、耗时明细、收尾原因、**逻辑请求数**、
  作品与正文 hash、提取方式与告警。长文本一律可折叠。
  **服务端未改动** —— 数据本来就都在返回里。盲选脱敏同时覆盖新字段。
- **中途输入 = 追加轮次**（用户第 3 条反馈，用户拍板取方案 1）。
  流式接口上无法向已发出的请求追加消息，所以"中途输入"实现为**新的一轮 attempt**：
  复用已有的重试机制，原 attempt 与原始输出完整保留、仍可单独下载。
  界面每张卡标注"第 N 轮"，"原始输出与提取结果"区**按轮次逐行列出每一次尝试**。
  下一轮只回放**真正发生过**的内容（上一轮 user 文本 + 上一轮原始正文，逐字节）；
  上一轮没有正文时**拒绝**追加，绝不伪造 assistant 消息。

### 工程改动（防止同类问题再发生）

- 抽出 `src/core/runtime.js`：宿主半边与开发服务器**共用同一份**执行管线。
  此前 `scripts/dev-server.mjs` 手抄了一份 `runCandidate`，缺了实时流缓冲 ——
  导致"推理过程被收回"这个缺陷只在真实 DSH 上出现、在零费用的开发服务器上复现不出来。
- `scripts/simulated-llm.mjs` 新增 `sim-reasoning`（先流式输出推理再输出正文），
  让"界面上出现推理过程"这条路径可以**零费用**复现。
- 测试从 77 个增加到 **95 个**（+12 设置、+6 追加轮次）；
  新增 4 个零费用验证脚本（`m2-regression-walkthrough` / `m2-screenshot-redraw-check` /
  `m2-capability-walkthrough` / `m2-rounds-walkthrough`）。

### 验收变化

`m2-four-walkthrough` 从 8 项扩到 **26 项**（新增展开配置与盲选脱敏断言）；
`m1-compare-walkthrough` 的 27 项在改动后重跑仍全绿。
**本轮没有发起任何付费的真实模型测试** —— 全部用模拟模型或已存在的实验数据验证。

## 0.2.0 —— 用户试玩反馈批次（阶段试玩，非完整 V1）

用户在试玩 v0.1.0-m1 后提了 4 条意见；其中两条实际是缺陷。全部已实现并实测。

修复的缺陷：
- **实时生成监控是死代码**：`runGeneration` 一直在发 `text-delta`，但宿主只取了时间戳、
  把正文片段丢掉；界面 `state.streams` 从未被写入。已接通并新增 `GET /experiments/:id/live`。
- **四候选对比挤成一条**：`.compare-grid` 写死两列。已按候选数自适应（2 列 / 两行两列）。

新增：
- **输出要求预设**：7 条常用要求（无依赖单 HTML 等），点一下追加、再点移除，不覆盖手写内容。
- **提示词优化器对接**：零侵入对接本机已装的 `dsh-prompt-optimizer`（走它自己的 HTTP API）。
  支持选档位与指定优化模型，结果先给用户看，失败一律 fail-open。见 `docs/optimizer-integration.md`。
- **"再添加一个"按钮**：自动挑一个没用过的模型，方便 3–4 套配置比对。
- **A06 取得决定性证据**：4 个真实候选 + 并发 2，实测最大同时 running 恒为 2、队列正确推进。

## 0.1.0 —— M1 首次可玩版（阶段试玩，非完整 V1）

首次用**两个不同的真实模型**跑通同一道题的完整对比流程（A02 / A30 的第一批真实证据）。

修正（全部来自 M1 的真实浏览器 / 真实模型实测）：
- 保存评价后"揭晓身份"按钮不出现，盲选流程卡住（`web/app.js` 的 `saveVote` 没有刷新按钮）。
- 隐藏配置身份期间，折叠的"配置差异"面板仍直接写出 provider / model（点开即泄露）。
- 揭晓后作品卡只显示候选名 A / B，看不出谁是谁；现在直接写出 provider / model。
- 开发服务器缺少 `runtime.llmOf`，`/models` 与 `/models/resolve` 返回 500，界面拿不到模型目录。

新增：
- M1 实测脚本 `scripts/m1-*.mjs` 与对应 npm 脚本（`m1:compare` / `m1:probe` / `m1:a02` / `m1:samples` / `m1:walkthrough`）。

## 0.0.1 （未发布，M0 开发中）
- 项目初始化：DSH 外部 bundle 插件骨架、自有 HTTP 服务、预览隔离、HTML 提取器、SQLite 存储。